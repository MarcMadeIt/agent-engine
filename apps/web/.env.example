# Where the agent-engine HTTP API is reachable (server-side only).
AGENT_API_URL=http://127.0.0.1:8787
# Same value as AGENT_API_KEY in the engine's .env. NEVER prefix with NEXT_PUBLIC_.
AGENT_API_KEY=
